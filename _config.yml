url: https://insert-your-url-here.invalid # replace with your webring's URL, which should end in .netlify.app (no slash afterwards)
include: [_redirects]
kramdown:
 syntax_highlighter: +nil+
 smart_quotes: "39,39,34,34"
github_repo_url: https://github.com/Conallia/Cool-people-webring # replace with your repository's URL, which should begin with https://github.com/ and should also have no slash at the end
